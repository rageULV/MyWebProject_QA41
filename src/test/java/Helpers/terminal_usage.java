package Helpers;

public class terminal_usage {

    //allure generate allure-results --clean -o allure-report (its with cleaning cash of old reports)
    //allure serve
    //allure generate allure-results -o allure-report (this one not cleaning and saves all test reports)

    





}
